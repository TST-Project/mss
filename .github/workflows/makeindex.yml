name: make-index
on: [push]
jobs:
  make-index:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.head_ref }}

      - run: |
          npm install fs
          npm install jsdom
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add index.html
          git commit -m "automatic index update"
          git push
